{
  "name": "smartframe",
  "version": "0.1.0",
  "description": "Script for downloading and viewing pictures on a rasberry pi in slideshow mode",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && babel src --out-dir dist",
    "lint": "eslint src",
    "start": "node dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Doug Miller (doug@dougmiller.io)",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.15.3",
    "babel-preset-es2015": "^6.3.13",
    "chalk": "^1.1.1",
    "googleapis": "^2.1.7",
    "prompt": "^0.2.14"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "eslint": "^3.13.1",
    "eslint-config-airbnb-base": "^11.0.1",
    "eslint-plugin-import": "^2.2.0"
  }
}
